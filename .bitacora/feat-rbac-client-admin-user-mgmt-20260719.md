### 2026-07-19 · innova67 · RBAC — client_admin gestiona su propio staff (#200)
- Qué cambió: se **separa** la capacidad *gestionar usuarios* de *config del agente*. (1) Nueva dep `require_user_manager` (platform_operator **o** client_admin) en [deps.py](src/server/modules/core/api/deps.py); los 4 endpoints de `/users` (list/create/role/delete) pasan de `require_platform_operator` a `require_user_manager`. `/agents` y settings **siguen** en `require_platform_operator`. (2) Guardas tenant-scoped de la matriz RBAC en [user_router.py](src/server/modules/core/api/user_router.py): un client_admin no puede cambiar el rol ni eliminar a un `platform_operator` (target `is_superuser` → 403 salvo actor operador); nadie cambia su propio rol (`PUT /{id}/role` sobre self → 400). (3) Anti-self-lockout en `PATCH /users/me`: `is_active=false` sobre uno mismo → 400. (4) `UserService.delete` → `remove_from_tenant` (tenant-scoped): `DELETE /users/{id}` quita la **membresía del tenant activo**; el `User` global se borra solo si era su última membresía (evita impacto cross-tenant). Se mantienen las guardas de #173 (no auto-baja, ≥1 platform_operator). El enum asignable por API sigue siendo solo `client_admin`/`staff` (alta de operadores queda script-only).
- Por qué: decisión de producto (2026-07-01) — Mirko (`client_admin`) debe administrar su propio staff sin acceder a la config del agente ni a otros tenants. Cierra el **paso 1** del split de #151 (umbrella RBAC); desbloquea el FE web #126.
- Spec/decisión que respeta: `docs/SPECS_MVP.md` §RBAC **actualizado primero** (doc-first): tabla de roles, distinción `client_admin` vs `staff` (= gestión de usuarios), matriz "quién cambia/elimina a quién", split `require_user_manager` ≠ `require_platform_operator`. Umbrella #151, sub-issue #200, base #173.
- Prueba local: `pytest` **378 passed** (test_users_roles: staff-403, client_admin gestiona su staff, no-tocar-operador 403, no-cambiar-rol-propio 400, PATCH /me no-self-deactivate 400, delete multi-tenant conserva la otra membresía, ≥1 operator; test_users actualizado a la nueva semántica). `ruff check` limpio · `mypy` Success (113 files).
- Commit:   (completar después del commit)
